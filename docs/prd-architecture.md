# 申论智能研习台（Shenlun Exam OS）产品手册与架构规格书

*版本：v2.5.0（正式发布版 · 生产就绪）*  
*编制日期：2026-09-21*  
*产品使命：拒绝通用 AI 的单次消费型线性对话，打造基于考场手写作答、采分点全真色谱穿透、个人记忆双向复利与跨试卷认知病灶追踪的申论专属智能操作系统。*

---

## 一、 需求规划 vs 当前系统完成度深度对账

本章节对原始需求规划（PRD）与当前已实装代码进行逐模块的深度对账与合理性判定：

| 功能模块 | 原始需求规划 (PRD) | 当前系统实际代码实现 | 完成度 | 架构与实现合理性判定 |
| :--- | :--- | :--- | :---: | :--- |
| **1. 物理作答录入** | 拒绝虚拟格纸等外观伪需求，支持线下手写作答后敲入，实时统计字数与格式初检。 | `web/index.html` + `app.js` 实时字数核算、字数红线（<800字/超限）及书名号硬伤前检。 | **100%** | **合理**。完全贴合真实考场手写节奏，无过度设计。 |
| **2. 题干与材料阅览** | 完整展开题干作答要求，给定材料全文连贯可读，支持真实 PDF 讲义上传提取。 | 支持直接上传带文字图层的 `.pdf`/`.md`/`.txt`，利用 `PyMuPDF` 毫秒级提取全文；材料面板采用考卷式连贯排版，去噪过滤页码。 | **100%** | **超出预期**。原规划仅支持纯文本，现已实装工业级 PyMuPDF 内存流解析，大模型读取 100% 无损原文。 |
| **3. 题型与Skill适配** | 题型自动分流，出厂 Skill 严禁出现真实老师/机构姓名以杜绝侵权，支持用户自建私有 Skill。 | 出厂封装中立官方阅卷规范（大五段专家/三轨专家/八大要素采点专家），代码库经自动化扫描确认 0 名师人名；题型切换自动适配默认 Skill；支持工坊两步自建私有 Skill。 | **100%** | **合理且合规**。完全消除法律版权隐患，题型联动顺畅。 |
| **4. 真实大模型批改** | 接入真实大模型，杜绝假数据与即兴幻觉。 | 废除任何前端假数据 Mock，后端严格直连 DeepSeek-V3 实时在线推理，前端加入明确的 3~6 秒实时调用加载态。 | **100%** | **合理**。端到端验证通过，模型打分严谨客观。 |
| **5. 多维色谱穿透定位** | 原词绿/大词黄/漏点虚红/臆造紫/流水账灰/抄袭黄闪烁，精准高亮。 | 采用 ADR 0005 确立的“子串引用锚定定位器 (Quote-Anchor SpanResolver)”，大模型输出字面原句，本地算法回填 `[start, end]` 物理字符坐标；本地 15-gram 算法检测 20% 抄袭红线。 | **100%** | **极度合理**。彻底规避了大模型数字符翻车的顽疾，位置 100% 精确。 |
| **6. 考场给分归因呈现** | 呈现评分明细、考官判语、逐句扣分归因。 | 页面实时渲染 **四维量化得分明细表**（立意/结构/论据/语言）、**多视角名师深度定档判语**、以及 **逐句诊断与扣分点穿透清单**。 | **100%** | **合理**。给分扣分理由透明具象，告别黑盒打分。 |
| **7. 认知病灶自反性闭环** | 跨试卷追踪思维基因缺陷，自动锁定顽固病灶，并在下次作答前置警报。 | 批改结果自动提取缺陷存入 IndexedDB `dossier` 表；动态计算历史犯错率；连续触碰自动标记【一级顽固病灶】并在作答页上方弹出置顶预警条。 | **100%** | **合理**。真正实现了“上一次的教训成为下一次下笔前的警钟”。 |
| **8. 个人记忆库双向复利** | 逆向激活率审计，示范文强制调用考生记忆库素材（带来源徽章），支持一键入库与 SM-2 翻卡。 | 纯前端 Mini-RAG 秒级召回已背素材；AI 逆向审计调用率并在示范文中强制注入带 `[来自记忆库: xxx]` 徽章；一类文支持鼠标划词即入库；诊断扣分项支持【一键存为避坑错题卡】。 | **100%** | **超出预期**。扣分项直接转为避坑卡排入背诵流，形成了强大的资产复利。 |
| **9. 3分钟靶向微练习** | 针对本次作答暴露的口语大白话，现场出题即刻秒判切除病灶。 | 后端自动提取口语原句生成改写小题；前端提供输入框；后端规则引擎秒判政务动宾大词命中度。 | **100%** | **合理**。3 分钟快速闭环，针对性极强。 |
| **10. 隐私安全与数据架构** | 服务端零留存、零数据库；用户数据 100% 存在浏览器本地；纯客户端持 Key (BYOK)。 | 采用前后端分离与客户端数据中枢（IndexedDB 6 大存储区）；服务端 FastAPI 纯内存无状态；支持单文件全量 JSON 导出与温和防丢提醒。 | **100%** | **合理**。隐私保护达到最高标准，零服务端存储运维负担。 |

---

## 二、 核心业务功能详解 (PRD)

```
┌────────────────────────────────────────────────────────────────────────────────────────┐
│                        考生在“申论智能研习台”的完整使用旅程                            │
│                                                                                        │
│  [1. 线下纸面作答] ──(真实试卷上手写，练时间感)──> [2. 敲入作答文本]                  │
│                                                          │                             │
│  ┌───────────────────────────────────────────────────────┴──────────────────────────┐  │
│  │ 浏览器本地客户端 (IndexedDB + 客户端检索引擎)                                     │  │
│  │  - 自动呈现预置真题材料 / 本地上传的 PDF 讲义纯净全文                             │  │
│  │  - 题型自动联动官方中立批改 Skill (大五段/三轨制/采点制)                           │  │
│  │  - 自动召回考生记忆库中与该题匹配的【已背名言/对策】 (Top-K)                      │  │
│  │  - 调取历史病灶数据，在作答框置顶显示【自反性前置警报】                           │  │
│  └───────────────────────────────────────┬──────────────────────────────────────────┘  │
│                                          │ HTTPS (Stateless Request / 透传 BYOK Key)   │
│  ┌───────────────────────────────────────▼──────────────────────────────────────────┐  │
│  │ 无状态 AI 算力服务端 (Stateless Fast Engine - 零数据留存)                         │  │
│  │  - 15-gram 滑动窗口计算材料抄袭率（>20%红线预检）                                 │  │
│  │  - 实时在线调度 DeepSeek-V3 运行考场级多维评审                                    │  │
│  │  - Quote-Anchor SpanResolver 将语义引用精准回填为物理字符坐标                     │  │
│  │  - 限制性一类文重构 (MAR)，优先融入考生背诵词库并打上徽章                         │  │
│  │  - 提取大白话瑕疵，现场生成 3 分钟针对性微练习                                    │  │
│  └───────────────────────────────────────┬──────────────────────────────────────────┘  │
│                                          │ 结构化 JSON 返回                            │
│  ┌───────────────────────────────────────▼──────────────────────────────────────────┐  │
│  │ 浏览器本地全真呈现与资产沉淀                                                      │  │
│  │  - 渲染字符级多维色谱（原词绿/大词黄/漏点红/臆造紫/流水账灰/抄袭黄闪烁）           │  │
│  │  - 渲染考场四维量化判分表、考官多视角判语、逐句扣分归因清单                       │  │
│  │  - 扣分项一键点击【📥 存为避坑错题卡】 / 范文【划词即存记忆库】                   │  │
│  │  - 真实将本次缺陷写入本地 IndexedDB `dossier`，动态刷新认知病灶图谱               │  │
│  │  - 现场作答 3 分钟微练习，即刻秒判切除病灶                                        │  │
│  └──────────────────────────────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 三、 系统技术架构规范 (Architecture)

### 3.1 本地浏览器存储中枢 (IndexedDB Schema)
数据库名：`ShenlunStudyDB`（Version: 2）：
1. `memories`：个人名言、特色对策与避坑错题卡（含 SM-2 调度字段 `repetitions, interval, ease, nextReview`）；
2. `submissions`：考生历史作答记录；
3. `reports`：完整色谱跨距、量化四维得分表、多视角判语及一类文示范；
4. `dossier`：跨试卷认知病灶档案（记录 `ERR_COPY_OVER_20`, `ERR_COLLOQUIAL`, `ERR_STRUCTURE_DEFECT` 等）；
5. `custom_skills`：考生在提炼工坊中自建的私有批改专家；
6. `private_kb`：考生上传的私密 PDF 提取文本、时政材料与个人笔记。

### 3.2 无状态算力后端架构 (FastAPI Engine)
1. **纯无状态设计**：服务器不配置数据库，不存储任何考生答卷与个人资产；
2. **严格 BYOK 密钥管理**：API Key 保存在考生本地浏览器，发起请求时透传，服务端仅在内存中转发，用完即焚；
3. **PyMuPDF 内存流解析**：`POST /api/extract-pdf` 在内存流中直接提取 PDF 的文字图层，毫秒级响应，零磁盘文件落地；
4. **子串引用锚定定位器 (SpanResolver)**：大模型仅输出字面引用原句，本地 Python 执行子串匹配与正则模糊对齐，计算精确 `[start, end]` 物理坐标；
5. **本地确定性抄袭红线预检**：本地运行 15 字符滑动窗口比对给定材料与作答文本，精准核算连续抄袭字数与全局抄袭率。
