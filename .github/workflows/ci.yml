name: CI Test Suite

on:
  push:
    branches: [ main, master, feature-* ]
  pull_request:
    branches: [ main, master ]

jobs:
  python-tests:
    name: Backend PyTest (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r server/requirements.txt

      - name: Run Backend Unit Tests
        run: |
          pytest -v

  frontend-tests:
    name: Frontend Logic & Pure Client-Side Tests (Node ${{ matrix.node-version }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: ["20.x", "22.x"]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Run All Pure Frontend Test Suites
        run: |
          node web/tests/test_dossier_metrics.js
          node web/tests/test_local_scanner.js
          node web/tests/test_local_drill.js
          node web/tests/test_exams_loader.js
          node web/tests/test_local_pdf.js
          node web/tests/test_api_client.js
          node web/tests/test_real_exam_subquestions.js
